# Normalize line endings so the repo is consistent across macOS, Linux, and Windows.
# Text files are stored with LF in the repo; Git converts on checkout per-OS.
* text=auto eol=lf

# Explicitly text
*.py    text
*.toml  text
*.md    text
*.lock  text

# Binary / archives — never touch
*.zip   binary
